// three-slicer-viewer/paint — the painting tool names the viewer and the slicer worker share.

/** The tools that fill on one click rather than paint along a drag: 'smart', 'bucket', 'triangle'. */
export const FILL_TOOLS: ReadonlySet<string>

/** The fills that spread across neighbouring facets and take a fill angle: 'smart', 'bucket'. */
export const ANGLE_FILL_TOOLS: ReadonlySet<string>
